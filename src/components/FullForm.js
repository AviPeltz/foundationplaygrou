
export default function FullForm({status}){
// status -> [empty, typing, submitting, success, error]

    return(
        <form>
            <h3>Form:</h3>
            <p>Form description</p>
            <textarea />
            <button>Submit</button>
        </form>
    );


}